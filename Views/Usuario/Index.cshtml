@model ICollection<UsuarioSummary>

<div class="row">
  <div class="col">
    <div class="card">
      <div class="card-header">
        <div class="card-title">@ViewData["title"]</div>
        <div class="card-tools">
          <a class="btn btn-success" asp-route="@AdminRoutes.Usuarios.CreateName">
            <em class="fas fa-plus"></em>
            Novo Usuário
          </a>
        </div>
      </div>
      <div class="card-body">
        @if (Model.Count <= 0)
        {
          <div>
            Nenhum usuário cadastrado.
          </div>
        }
        else
        {
          <table class="table table-condensed">
            <thead>
              <tr>
                <th>#</th>
                <th>Nome Completo</th>
                <th>E-mail</th>
                <th>Ações</th>
              </tr>
            </thead>
            <tbody>
              @foreach (var usuario in Model)
              {
                <tr>
                  <td>@usuario.Id</td>
                  <td>@usuario.NomeCompleto</td>
                  <td>@usuario.Email</td>
                  <td>
                    <a
                      class="btn btn-sm btn-warning"
                      asp-route="@AdminRoutes.Usuarios.UpdateByIdName"
                    >
                      Editar
                    </a>
                    <a
                      class="btn btn-sm btn-danger"
                      onclick="return confirm('Tem certeja que deseja excluir este serviço?')"
                      asp-route="@AdminRoutes.Usuarios.DeleteByIdName"
                    >
                      Excluir
                    </a>
                  </td>
                </tr>
              }
            </tbody>
          </table>
        }
      </div>
    </div>
  </div>
</div>